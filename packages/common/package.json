{
  "name": "@todo-app/common",
  "version": "1.0.0",
  "private": true,
  "main": "build/index.js",
  "typings": "build/index.d.ts",
  "scripts": {
    "build": "rm -rf build && tsc",
    "lint": "eslint --fix --ext .ts,.tsx .",
    "format": "prettier --write '*/**/*.{ts,js,css,md,json}'"
  },
  "dependencies": {
    "class-validator": "^0.12.0",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.2.24"
  },
  "devDependencies": {
    "eslint": "6.8.0",
    "typescript": "^3.9.5"
  }
}
